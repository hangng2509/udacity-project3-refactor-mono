apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: api-feed
  name: api-feed
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-feed
  template:
    metadata:
      labels:
        app: api-feed
    spec:
      containers:
      - name: api-feed
        image: 014091804/udagram-api-feed
        ports:
          - containerPort: 8080
        envFrom:
          - configMapRef:
              name: udagram-067328141418-env
          - secretRef:
              name: udagram-db-secret
        resources:
          requests:
            memory: "64Mi"
            cpu: "250m"
          limits:
            memory: "128Mi"
            cpu: "500m"              
        volumeMounts:
        - name: aws-cre
          mountPath: "/root/.aws/"
          readOnly: true        
      volumes:
      - name: aws-cre
        secret:
          secretName: aws-cre      